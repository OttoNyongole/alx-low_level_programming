#!/bin/bash
#include <stdio.h>
#include<conio.h>
#include <stdlib.h>
/**Declaration of function main
 *main function is the first function to be excuted by the program
 */
int main ()
{
	printf("The rondom number is: %d",rand());
	printf("The rondom number is: %d",rand());
	printf("The rondom number is: %d",rand());
	printf("The rondom number is: %d",rand());

return 0;
}

